<h1>Realizar transferência</h1>

<%= form_for Transfer.new, html: {autocomplete: "off"}, url: transfer_path, method: :post do |t| %>

  <input name="transfer[user_id]" id="transfer_user_id" type="hidden" value="<%= current_user.id %>">

  <div class="field">
    <label>Conta origem</label>
    <p><%= t.collection_select(:origin_account, Account.where(:users_id => current_user.id, :status => "Ativa"), :id, :number, prompt: "Selecione a conta") %></p>
  </div>

  <div class="field">
    <label>Conta destino</label>
    <p><%= t.collection_select(:destiny_account, Account.where(:users_id => current_user.id, :status => "Ativa"), :id, :number, prompt: "Selecione a conta") %></p>
  </div>

  <div class="field">
    <label>Valor</label>
    <p><%= t.number_field :value %></p>
  </div>

  <div class="field">
    <label>Digite sua senha:</label>
    <p><%= password_field_tag :password %></p>
  </div>

  <div class="field">
    <h4>Atenção:</h4>
    <p>De segunda a sexta das 9h as 18h a taxa de transferência é 5 reais. Fora desse horário, a taxa é 7 reais.</p>
    <p>Transferências de valor superior a 1000 reais tem um adicional de 10 reais na taxa de transferência.</p>
  </div>

  <div class="field">
    <%= t.submit "Transferir" %>
  </div>

<% end %>

<%= link_to '<button>Voltar</button>'.html_safe, accounts_path %>
